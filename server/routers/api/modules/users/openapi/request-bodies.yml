user_create:
  description: Création de l'utilisateur
  required: true
  content:
    application/json:
      schema:
        $ref: "schemas.yml#/user_create"

user_update:
  description: Mise à jour de l'utilisateur
  required: true
  content:
    application/json:
      schema:
        $ref: "schemas.yml#/user_update"
